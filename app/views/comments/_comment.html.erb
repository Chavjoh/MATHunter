<tr>
	<td class="title">
		<p><strong><%= comment.user.email[/[^@]+/].sub('.', ' ').split.map(&:capitalize).join(' ') %></strong></p>
	</td>
	<td>
		<em><%= simple_format(comment.content) %></em>
	</td>
</tr>